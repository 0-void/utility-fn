import { wait } from 'index'

jest.useFakeTimers()

describe('wait for specified amount of time', () => {
  test('It should wait for 5 seconds', async () => {
    // await wait(5000) // expect(setTimeout).toBeCalledWith(50000)
    // expect(setTimeout).toHaveBeenCalledTimes(1)
    // expect(setTimeout).toHaveBeenLastCalledWith(expect.any(Function), 1000)
  })
})
